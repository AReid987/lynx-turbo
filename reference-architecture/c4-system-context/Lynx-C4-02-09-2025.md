---
config:
  theme: neo-dark
---
C4Container
title Container diagram for Lynx System
Person(user, "User", "A user of the Lynx system.")
Container_Boundary(c1, "Lynx System") {
    Container(web_ui, "WebUI", "Next.js", "Provides the web interface for users.")
    Container(docs, "Docs", "Next.js", "Provides documentation for the Lynx system.")
    Container(lynx_core, "Lynx Core", "Python", "Handles core business logic and data processing.")
    Container(ai_gateway, "AI Gateway", "FastAPI", "Unified AI API providing access to all LLM Inference.")
    ContainerDb(database, "Database", "DataStax Astra DB with Neon BD", "Stores user data, application data, and other persistent information.")
    Container(drizzle_orm, "Drizzle ORM", "JavaScript/TypeScript", "Handles database interactions using Drizzle ORM.")
    Container(clerk, "Clerk", "Authorization Service", "Handles user authorization and authentication.")
}
System_Ext(externalSystem1, "External System 1", "An external system that Lynx interacts with.")
System_Ext(externalSystem2, "External System 2", "Another external system that Lynx interacts with.")
Rel(user, web_ui, "Uses", "HTTPS")
Rel(user, docs, "Uses", "HTTPS")
Rel(web_ui, lynx_core, "Sends requests to", "HTTPS")
Rel(web_ui, clerk, "Handles authorization with", "HTTPS")
Rel(lynx_core, database, "Reads from and writes to", "AstraPy")
Rel(lynx_core, ai_gateway, "Interacts with", "HTTPS")
Rel(lynx_core, drizzle_orm, "Uses for database interactions", "HTTPS")
Rel(ai_gateway, externalSystem1, "Interacts with", "HTTPS")
Rel(ai_gateway, externalSystem2, "Interacts with", "HTTPS")
UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
